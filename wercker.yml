box: wercker/python

build:
  steps:
    - wercker/pip-install@0.0.6:
        packages_list: "ansible>=1.2"

    - script:
        name: Generate an inventory file
        code: echo localhost > inventory
        
    - script:
        name: Check Ansible syntax
        code: ansible-playbook -i inventory --syntax-check test.yml
